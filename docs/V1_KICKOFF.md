You are working in xeque-engine, a Rust chess-engine collection shipped as a native UCI binary, a WebAssembly module, and a five-mode React web UI deployed to GitHub Pages. Produce a COMPLETE plan to ship the FIRST RELEASE (v1). Plan only — do NOT write/edit code or docs until I approve. Read README.md, docs/ENGINES.md (+ any ROADMAP), CLAUDE.md, PORTFOLIO_STANDARD.md (just reset/created to reflect true current state); then explore crates/xeque-engines/src/lib.rs (the registry — note THREE engines shipped: v0_random, v1_minimax, v2_alphabeta), crates/xeque-core/src/engine.rs (the Engine trait), crates/xeque-uci/src/main.rs, crates/xeque-wasm/src/lib.rs, web/src/App.tsx, web/src/engine/api.ts, and both .github/workflows/ci.yml + deploy.yml. v1 is "ship what exists, honestly and beautifully," NOT "build the next engine." IN: ship the three engines, UCI binary, WASM Session, all five web modes (Play, Lab, Analysis, Arena, Perft), the perft regression, existing CI/deploy — with docs + hygiene brought to a portfolio bar. OUT: implementing any new engine (v3_iterative_ordering → v7_nnue/v7_mcts), functional time controls / restoring the removed Think Time control, the cutechess-cli + SPRT harness, an ELO baseline, any edit to the frozen existing engines. Plan must cover: (1) bring every doc into truth — no remaining "two engines" claim and no listing of v2_alphabeta as backlog anywhere (check README line ~18, docs/ARCHITECTURE.md backlog "v2..v7"→"v3..v7", CLAUDE.md layout block); (2) add hero media (screenshot/GIF of the live UI) to the README; (3) confirm green locally + in CI — cargo fmt --check, clippy --workspace --all-targets -D warnings, cargo test --workspace --exclude xeque-bench, the release perft regression, the wasm-pack build, npx tsc -b (verify before touching tsconfig.node.json — its composite config is valid); (4) verify the README quick-start works verbatim; (5) a CHANGELOG entry + a git tag. Honor CLAUDE.md hard rules: Rust-only engine code, never re-implement chess rules in TS, never edit existing engines in place, no Python/Pygame. Bar = PORTFOLIO_STANDARD.md. Deliver a sequenced plan with the files each step touches, the verification command per step, and any risks/ambiguities to resolve. Present and wait for my approval.
